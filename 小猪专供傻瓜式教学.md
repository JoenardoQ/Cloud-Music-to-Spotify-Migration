# 小猪专供傻瓜式教学

[English](https://github.com/JoenardoQ/Cloud-Music-to-Spotify-Migration/blob/main/BEGINNER_GUIDE.md)

这份教程给第一次使用 Cloud Playlist Bridge 的人。目标是把自己的网易云音乐歌单迁移
到 Spotify，同时避免在正式开发目录里做实验。

## 先记住三件事

1. 在单独的试验目录安装和运行，不要直接使用维护源码的主目录。
2. `plan` 只分析和生成计划；只有 `apply` 或 App 中的“创建 Spotify 歌单”会写入
   Spotify。
3. 不要分享 `.state/spotify-token.json`、Spotify Client ID 之外的凭据或任何 Cookie。
   本项目不需要 Spotify Client Secret。

## 第一步：准备 Spotify

1. 确认 Spotify 账户满足当前 Developer 应用要求。
2. 在 Spotify Developer Dashboard 创建启用 Web API 的应用。
3. 精确添加 Redirect URI：

   ```text
   http://127.0.0.1:8888/callback
   ```

4. 复制 Client ID。不要创建、粘贴或提交 Client Secret。

## 第二步：创建独立试验场

最稳妥的方法是另外克隆一份仓库。这样实验产生的虚拟环境、缓存和报告不会混入维护
源码目录。

Linux 或 macOS：

```bash
cd ~
git clone https://github.com/JoenardoQ/Cloud-Music-to-Spotify-Migration.git CloudPlaylistBridge-Lab
cd CloudPlaylistBridge-Lab
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e .
```

Windows PowerShell：

```powershell
cd $HOME
git clone https://github.com/JoenardoQ/Cloud-Music-to-Spotify-Migration.git CloudPlaylistBridge-Lab
cd CloudPlaylistBridge-Lab
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e .
```

看到 `Successfully installed cloud-playlist-bridge-0.5.0` 就表示安装成功。若提示 pip 有新
版本，可以暂时忽略；它不影响迁移。

## 第三步 A：使用图形 App

在已激活虚拟环境的终端运行：

```bash
cloud-playlist-bridge app --no-browser
```

保持终端开启，然后在浏览器访问：

[http://127.0.0.1:8765/](http://127.0.0.1:8765/)

页面中的操作顺序：

1. 输入网易云歌单 ID 或分享 URL。
2. 输入 Spotify Client ID。
3. 默认不填写 `api-enhanced` 地址；只有在你已自行运行该服务时才填写。
4. 点击“分析歌单”。首次使用时按提示完成 Spotify OAuth 授权。
5. 检查自动匹配、歧义和跳过结果。
6. 确认无误后，点击“创建 Spotify 歌单”。这一步才会写入 Spotify。

终端必须保持运行。停止 App 时回到终端按 `Ctrl+C`。

## 第三步 B：完全使用命令行

不想使用 App 页面时，可以直接执行两阶段 CLI。

Linux 或 macOS，先生成计划：

```bash
./.venv/bin/python -m cloud_playlist_bridge plan 'https://music.163.com/playlist?id=123456789' --spotify-client-id YOUR_CLIENT_ID
```

Windows 命令提示符或 PowerShell，先生成计划：

```bat
.venv\Scripts\python.exe -m cloud_playlist_bridge plan "https://music.163.com/playlist?id=123456789" --spotify-client-id YOUR_CLIENT_ID
```

命令会在 `reports/` 生成：

- `*.plan.json`：经过校验的固定执行计划；
- `*.csv`：全部歌曲的审计报告；
- `*.manual.csv`：需要手动处理的歌曲。

检查报告后再执行写入。

Linux 或 macOS：

```bash
./.venv/bin/python -m cloud_playlist_bridge apply reports/NAME.plan.json --spotify-client-id YOUR_CLIENT_ID --private
```

Windows 命令提示符或 PowerShell：

```bat
.venv\Scripts\python.exe -m cloud_playlist_bridge apply reports\NAME.plan.json --spotify-client-id YOUR_CLIENT_ID --private
```

把 `NAME` 换成实际文件名。删除 `--private` 会创建公开歌单。

## 哪些文件只属于本地实验

以下内容不应加入发行提交，项目的 `.gitignore` 已排除它们：

- `.venv/`：本机 Python 虚拟环境；
- `.state/`：检查点和 Spotify token；
- `reports/`：迁移计划及 CSV 报告；
- `dist/`、`build/`、`*.egg-info/`：构建产物和安装元数据；
- `__pycache__/`、`*.pyc`：Python 字节码缓存。

在维护仓库工作前后都运行：

```bash
git status --short
git status --ignored --short
```

第一条命令没有输出表示已跟踪源码没有改动。第二条命令中以 `!!` 显示的项目是被忽略
的本地文件，不会被普通 `git add` 加入提交，但仍可能包含隐私数据，不能分享。

## 常见问题

### `Address already in use`

端口 `8765` 已被其他程序占用。先检查是否已有 App 正在运行；也可以换端口：

```bash
cloud-playlist-bridge app --no-browser --port 8766
```

然后打开 `http://127.0.0.1:8766/`。App 页面端口与 Spotify OAuth 的 `8888` 回调端口
不是同一个端口。

### `gio: Operation not supported`

App 已经启动，只是 WSL 或无桌面 Linux 无法自动打开浏览器。使用 `--no-browser`，再
手动打开终端显示的地址。

### `cloud-playlist-bridge: command not found`

虚拟环境没有激活，或当前终端不是安装时的环境。可以跳过命令入口，直接运行：

```bash
./.venv/bin/python -m cloud_playlist_bridge --help
```

Windows 使用：

```bat
.venv\Scripts\python.exe -m cloud_playlist_bridge --help
```

### 同时显示 `(.venv) (base)`

这表示 Python 虚拟环境和 Conda base 提示同时存在，通常不影响运行。用以下命令确认
实际解释器位于当前项目的 `.venv`：

```bash
python -c "import sys; print(sys.executable)"
```

### Spotify 提示 Redirect URI 不匹配

Developer Dashboard 中的地址必须与命令使用的地址逐字符一致：
`http://127.0.0.1:8888/callback`。

### 想清空试验场

先确认迁移报告和手动名单是否需要保留，然后关闭 App。只删除你专门创建的
`CloudPlaylistBridge-Lab` 目录；不要对主目录、工作区根目录或不确定的路径执行递归
删除命令。

## 最后检查

开始真实迁移前，确认：

- 当前目录是单独的 `CloudPlaylistBridge-Lab`；
- Spotify Redirect URI 已登记；
- Client ID 正确，未使用 Client Secret；
- 已先运行 `plan` 或点击“分析歌单”；
- 已查看跳过和歧义歌曲；
- 只有在接受结果后才执行 `apply` 或点击创建按钮。
