# 小猪专供傻瓜式教学

[English](https://github.com/JoenardoQ/Cloud-Music-to-Spotify-Migration/blob/main/BEGINNER_GUIDE.md)

这份教程写给不懂编程、Git 和命令行的普通用户。你只需要会下载文件、复制命令和打开
网页。请从头到尾按顺序操作，不要跳步。

## 开始前准备

你需要：

- 一台 Windows、macOS 或 Ubuntu 电脑；
- Python 3.11 或更高版本；
- Spotify Premium 账户；
- 一个公开可访问的网易云音乐歌单。

大歌单的匹配过程可能需要较长时间。

[Spotify Web API](https://developer.spotify.com/documentation/web-api) 目前要求用户使用
Premium。这个工具不会下载歌曲，只会把歌单中的歌曲信息匹配到 Spotify。

## 第一步：下载项目 ZIP

1. 打开 [项目下载页面](https://github.com/JoenardoQ/Cloud-Music-to-Spotify-Migration/archive/refs/heads/main.zip)。
2. 浏览器会下载一个 ZIP 压缩包。
3. 解压 ZIP，并把文件夹放到容易找到的位置，例如“下载”或“桌面”。
4. 这个解压出来的文件夹就是你的独立试验场。后续操作全部在这里完成，用坏了也可以
   重新下载。

## 第二步：安装 Python

### Windows

1. 打开 [Python Windows 下载页](https://www.python.org/downloads/windows/)。
2. 下载并安装 Python 3.11 或更高版本。
3. 如果安装器显示 **Add python.exe to PATH**，请勾选它。

### macOS

打开 [Python macOS 下载页](https://www.python.org/downloads/macos/)，下载并安装
Python 3.11 或更高版本。

### Ubuntu

打开“终端”，复制下面这行并按回车：

```bash
sudo apt update && sudo apt install -y python3 python3-venv
```

系统要求输入密码时，输入电脑登录密码并按回车。输入过程中屏幕不显示字符是正常的。

## 第三步：在项目文件夹中打开终端

### Windows

1. 用文件资源管理器打开刚才解压的项目文件夹。
2. 点击窗口顶部的地址栏，输入 `powershell`，然后按回车。
3. 出现蓝色或黑色窗口后，不要关闭。

### macOS

1. 打开“终端”应用。
2. 输入 `cd `，注意 `cd` 后有一个空格。
3. 把解压后的项目文件夹拖进终端窗口，然后按回车。

### Ubuntu

在解压后的项目文件夹空白处点击右键，选择“在终端中打开”。

## 第四步：安装应用

只复制与你的系统对应的两行命令，每复制一行都按一次回车，并等待它执行完成。

### Windows

```powershell
py -3 -m venv .venv
.venv\Scripts\python.exe -m pip install .
```

### macOS 或 Ubuntu

```bash
python3 -m venv .venv
./.venv/bin/python -m pip install .
```

看到 `Successfully installed cloud-playlist-bridge-0.5.0` 就表示安装成功。看到 pip 可以
升级的提示也没关系，不需要升级。

## 第五步：准备 Spotify Client ID

1. 打开 [Spotify Developer Dashboard](https://developer.spotify.com/dashboard)。
2. 使用你的 Spotify Premium 账户登录。
3. 点击 **Create app**。
4. App name 可以填写 `Cloud Playlist Bridge`，Description 可以填写
   `Personal playlist migration`。
5. [Spotify 要求](https://developer.spotify.com/documentation/web-api/concepts/redirect_uri)
   Redirect URI 必须逐字填写：

   ```text
   http://127.0.0.1:8888/callback
   ```

6. API 选择 **Web API**，勾选条款，然后创建或保存应用。
7. 进入应用设置，找到并复制 **Client ID**。

不要复制、输入或分享 Client Secret。本项目不需要它。Spotify 要求回调地址完全匹配，
不要把 `127.0.0.1` 改成 `localhost`，也不要漏掉 `/callback`。

## 第六步：启动应用

回到刚才没有关闭的终端，复制与你的系统对应的命令。

### Windows

```powershell
.venv\Scripts\python.exe -m cloud_playlist_bridge app --no-browser
```

### macOS 或 Ubuntu

```bash
./.venv/bin/python -m cloud_playlist_bridge app --no-browser
```

看到 `Cloud Playlist Bridge App：http://127.0.0.1:8765/` 表示启动成功。保持终端开启，
然后点击或在浏览器输入：

[http://127.0.0.1:8765/](http://127.0.0.1:8765/)

## 第七步：迁移歌单

1. 在“网易云歌单”输入框粘贴歌单分享链接或歌单 ID。
2. 在“Spotify Client ID”输入框粘贴第五步复制的 Client ID。
3. `api-enhanced` 地址先留空。
4. 点击“分析歌单”。
5. 浏览器打开 Spotify 授权页后，登录并同意授权。
6. 等待分析完成，检查匹配和跳过数量。
7. 只有确认结果后，才点击“创建 Spotify 歌单”。

“分析歌单”不会创建 Spotify 歌单；最后的创建按钮才会写入 Spotify。匹配不确定的歌曲
会被跳过，并保存在手动添加 CSV 中，不会偷偷加入错误歌曲。

## 用完后怎么关闭

回到运行应用的终端，按 `Ctrl+C`。看到“App 已停止”后可以关闭终端。

下次使用不需要重新安装。进入同一个项目文件夹打开终端，直接重复第六步即可。

## 常见错误

### `Address already in use`

端口已被占用，可能是应用已经在另一个终端运行。可以直接尝试打开
`http://127.0.0.1:8765/`。如果打不开，换一个端口：

Windows：

```powershell
.venv\Scripts\python.exe -m cloud_playlist_bridge app --no-browser --port 8766
```

macOS 或 Ubuntu：

```bash
./.venv/bin/python -m cloud_playlist_bridge app --no-browser --port 8766
```

然后打开 `http://127.0.0.1:8766/`。

### `gio: Operation not supported`

这只表示系统不能自动打开浏览器，应用通常已经正常运行。手动打开终端显示的网址即可。

### `py`、`python3` 或 `python` 找不到

Python 没有安装成功。重新执行第二步，安装完成后关闭终端，再重新打开。

### Spotify 提示 Redirect URI 不匹配

回到 Spotify Developer Dashboard，确认地址逐字等于
`http://127.0.0.1:8888/callback`，保存后重试。

### 网易云歌单读取失败

先确认歌单可以在未登录的浏览器窗口中打开。私有、仅自己可见或需要登录的歌单无法用
默认方式读取。

## 隐私提醒

项目文件夹中的 `.state` 保存 Spotify 登录令牌，`reports` 保存歌单报告。不要把这两个
文件夹发给别人，也不要上传到网盘或公开仓库。如果不再使用，先保存需要的报告，关闭
应用后再删除整个独立试验文件夹。
